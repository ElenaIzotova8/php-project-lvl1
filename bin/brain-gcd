#!/usr/bin/env php
<?php

$autoloadPath1 = __DIR__ . '/../../../autoload.php';
$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';
if (file_exists($autoloadPath1)) {
    require_once $autoloadPath1;
} else {
    require_once $autoloadPath2;
}

use function cli\line;
use function BrainGames\Cli\welcome;
use function BrainGames\Cli\question;
use function BrainGames\Cli\gcd;

$instr = 'Find the greatest common divisor of given numbers.';
$name = welcome($instr);

for ($i = 1; $i < 4; $i++)
    {
        $number1 = rand(1, 100);
        $number2 = rand(1, 100);
        $number = $number1 . ' ' . $number2;


        $correctAnswer = (string) gcd($number1, $number2);
    
        $iscorrect = question($number, $correctAnswer);
        if (!$iscorrect) {
            line("Let's try again, %s!", $name);
            break;
        } else {
            if ($i === 3) {
                line("Congratulations, %s!", $name); 
            }
        }
    }